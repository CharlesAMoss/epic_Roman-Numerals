describe('roman', function() {
  it("inputs a number and outputs a string", function() {
    expect(roman(1999)).to.be.an('array');
  });

  // it("is true for most years divisible by 4", function() {
    // expect(leapYear(2012)).to.equal(true);
  // });
});
